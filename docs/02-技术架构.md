第二章：技术架构
================================================================================

索引
================================================================================
2.1 前端技术栈
2.2 后端技术栈
2.3 数据库
2.4 实时通讯
2.5 文件存储
2.6 域名与服务器
2.7 职位编号生成规则

2.1 前端技术栈
--------------
框架：uniapp（跨平台开发）
UI风格：拟态设计（Neumorphism）
- 大圆角边框
- 柔和渐变色彩
- 阴影凸起效果
组件库：基于官方组件库二次开发
图标系统：SVG矢量图标

2.2 后端技术栈
--------------
语言：PHP 8.2
框架：Laravel 11.x
API风格：RESTful
部署路径：服务器根目录/public

【框架变更说明】
- 后端已从 ThinkPHP 6.x 完整迁移为 Laravel 11.x，运行环境要求 PHP 8.2+、MySQL 8.0+、Redis 6.0+
- 应用启动入口与框架装配遵循 Laravel 11 极简骨架：HTTP/Console 路由、异常处理、中间件注册统一通过 bootstrap/app.php 组织
- API 路由以 routes/api.php 为主；命令行任务与调度以 routes/console.php 和 Artisan Command 为主；服务提供者清单位于 bootstrap/providers.php
- 请求校验统一使用 FormRequest 或 Validator；控制器仅负责参数编排、权限校验、事务调度与响应封装，不再沿用 ThinkPHP 控制器/验证器写法
- 数据访问统一使用 Eloquent ORM、Query Builder、DB 事务、Schema Migration、Seeder、Factory 等 Laravel 标准能力，不再使用 ThinkPHP Model/Migrator 语法
- 认证体系统一为 Laravel 11 + JWT Bearer Token（用户端/管理员端通过不同 guard 或中间件区分），接口请求头统一使用 Authorization: Bearer {token}
- 缓存、队列、事件、文件存储、日志、异常处理均按 Laravel 组件约定落地：config/* 配置 + .env 驱动切换 + Artisan 命令管理
- 部署、定时任务、队列消费、WebSocket 常驻进程等运维操作均以 Laravel 11 项目结构为基准，不再沿用 ThinkPHP 的目录和启动方式

2.3 数据库
----------
类型：MySQL 8.0+
连接信息：
- 主机：localhost
- 数据库名：m_mokag_top
- 账号：m_mokag_top
- 密码：m_mokag_top
字符集：utf8mb4
排序规则：utf8mb4_unicode_ci

【数据库配置 - Laravel 11.x】
数据库配置位于 config/database.php，环境变量在 .env 文件：
```
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=m_mokag_top
DB_USERNAME=m_mokag_top
DB_PASSWORD=m_mokag_top
DB_CHARSET=utf8mb4
DB_COLLATION=utf8mb4_unicode_ci
```

【Laravel Migration 说明】
- 迁移文件位于 database/migrations/，数据填充位于 database/seeders/，模型工厂位于 database/factories/
- 使用 `php artisan migrate` 执行迁移，使用 `php artisan migrate:rollback` / `php artisan migrate:fresh --seed` 进行回滚或重建
- 表结构定义统一使用 `Schema`、`Blueprint`、外键约束、索引、唯一索引、软删除字段与时间戳字段；默认主键推荐使用 `$table->id()`
- 业务初始化数据通过 Seeder 管理，测试/演示数据通过 Factory 生成
- 涉及 JSON 字段、全文索引、函数索引等能力时，按 MySQL 8.0+ 特性设计并在迁移中显式声明
- 与 ThinkPHP 迁移差异：
  - Laravel 使用 `Illuminate\Support\Facades\Schema` 与 `Blueprint` 定义结构，不使用 `think\migration\Migrator`
  - Laravel 迁移文件包含 `up()` 与 `down()`，必须保证可回滚
  - Laravel 通过 Seeder / Factory / Artisan 命令串联初始化流程，而不是在迁移中混入大量业务数据写入
  - 生产环境变更应遵循"先迁移、再部署、再执行缓存刷新/队列重启"的 Laravel 发布流程

2.4 实时通讯
------------
技术方案：Workerman + Gateway Worker
通讯协议：WebSocket (ws://)
进程架构：
- Gateway进程：维护客户端连接
- BusinessWorker进程：处理业务逻辑
- Register进程：注册中心协调
心跳机制：客户端30秒心跳包

WebSocket连接与鉴权规范：

【连接地址】
- HTTP环境下：ws://m.mokag.top/ws
- HTTPS环境下：wss://m.mokag.top/ws
- Nginx 对外统一暴露 `/ws` WebSocket 入口，内部反向代理到 GatewayWorker/Workerman 监听端口（如 127.0.0.1:7272）
- 连接时必须携带鉴权 Token（URL 参数方式）：
  ws://m.mokag.top/ws?ws_token={ws_token}
  其中 ws_token 通过 GET /api/ws/config 接口获取

【鉴权流程】
1. 用户登录成功后，调用 GET /api/ws/config 获取 ws_token
2. 建立WebSocket连接时，将 ws_token 作为URL参数携带
3. 服务端验证 ws_token 有效性，验证通过后建立连接
4. 鉴权失败时服务端立即断开连接，返回错误码 9001（WebSocket专用错误码）

【WebSocket事件契约总则】
- 客户端→服务端消息统一使用 `action`、`data`、`ts`、`request_id` 四个顶层字段；服务端→客户端消息统一使用 `event`、`data`、`ts`、`request_id` 四个顶层字段
- 错误事件必须额外返回 `code`、`msg`；若为对某次客户端请求的回执，必须原样带回对应 `request_id`
- 事件命名统一使用点分命名：`message.created`、`message.read`、`conversation.updated`、`job.updated`、`application.updated`、`notification.created`、`system.kickout`
- 客户端指令统一使用 `action` 表达，例如：`ping`、`message.send`、`message.read`、`join.conversation`、`leave.conversation`、`message.recall`、`message.delete`
- 握手鉴权仅允许使用 `ws_token`；若对应 `device_session_id` 失效，服务端必须先推送 `system.kickout` 事件，再主动断开连接
- 断线重连后，客户端应基于最近游标或最后消息ID通过 HTTP 接口拉取增量数据；WebSocket 通道不承担离线全量补发职责
- 心跳统一为客户端发送 `ping`，服务端返回 `pong`；若连续 90 秒未收到有效心跳或响应，可判定连接失效
- 下文旧示例中如仍使用 `type` 字段，仅作为历史兼容示意；实施时统一以上述 `action` / `event` 契约为准
- 旧事件名与统一契约映射关系：`heartbeat` -> `ping`，`heartbeat_ack` -> `pong`，`message` -> `message.send`，`read` -> `message.read`，`join` -> `join.conversation`，`leave` -> `leave.conversation`，`recall` -> `message.recall`，`delete` -> `message.delete`，`new_message` -> `message.created`，`message_ack` -> `message.sent`，`read_receipt` -> `message.read`，`notification` -> `notification.created`，`message_recalled` -> `message.recalled`，`online_status` -> `presence.updated`，`online_count` -> `presence.count`，`error` -> `system.error`
- 若服务端因登录失效、设备被移除、密码变更、管理员强制下线等原因关闭连接，必须先发送 `system.kickout` 事件（包含 `code`、`msg`、`request_id` 可空、`ts`、`reason`），再断开连接
- 写操作（如 `message.send`、`message.read`、`message.recall`、`message.delete`）必须返回对应回执事件，并原样回传 `request_id`，前端不得仅凭是否收到广播推断请求成功
- `request_id` 由客户端生成并在单设备会话内保持唯一；`ts` 使用 Unix 时间戳秒或毫秒，但全链路必须统一一种精度
- 重连成功后，客户端应携带最近游标通过 HTTP 历史消息/通知接口拉取增量；若无游标则按最新会话列表重新同步，不再定义 `missed_messages` 这类 WebSocket 离线补偿专用事件
- 心跳超时阈值统一为 90 秒；超过阈值后服务端可直接断开连接，客户端按重连策略重新建立连接并走 HTTP 补偿
- WebSocket 事件仅承担实时通知与回执职责；历史列表、断线补偿、全量同步统一走 HTTP 接口
- 所有 WebSocket 事件中的业务标识（如 `conversation_key`、`file_id`、`job_id`）必须与 HTTP 接口对外字段口径保持一致，不得另起别名
- 对需要鉴权失败、频率限制、权限不足、内容违规等错误场景，统一使用 `system.error` 事件返回 9000-9999 范围内错误码
- 以下旧版 `type` 示例仅保留字段示意，真实落地时按上述映射改为 `action` / `event` 字段
- 示例信封：客户端请求 `{ "action": "message.send", "request_id": "req_123", "ts": 1709692800, "data": { ... } }`；服务端回执 `{ "event": "message.sent", "request_id": "req_123", "ts": 1709692801, "data": { ... } }`
- 错误示例：`{ "event": "system.error", "request_id": "req_123", "ts": 1709692801, "code": 9003, "msg": "发送过于频繁", "retry_after": 30, "data": null }`
- 强制下线示例：`{ "event": "system.kickout", "request_id": null, "ts": 1709692801, "code": 9001, "msg": "登录状态已失效", "data": { "reason": "device_session_invalid" } }`
- 若客户端未携带 `request_id`，服务端仍可广播业务事件，但对于需要明确成功/失败结果的写操作，客户端必须补充 `request_id` 后方可发起
- 前端收到 `system.kickout` 或鉴权失败错误后，必须立即清理本地对应登录态与 ws 连接状态，并重新走 HTTP 登录/刷新配置流程
- 同一 `device_session_id` 下如重复建立连接，服务端应按最新连接生效策略处理，并向旧连接发送 `system.kickout` 后断开，避免多连接状态错乱
- 对消息发送、已读、撤回、删除等需要幂等保证的动作，服务端应结合 `request_id`、`client_msg_id` 或业务唯一键做去重
- 对事件顺序敏感的场景（消息、已读、撤回），客户端应以服务端时间和业务主键为准进行幂等合并，避免因网络抖动导致乱序展示
- 需要前端确认收到但不要求强一致业务处理的广播事件，可不返回单独确认包；但涉及用户可见结果变更的写操作必须有回执
- 若后续新增事件，必须继续遵循点分命名、统一信封、错误码范围、`request_id` 回执、HTTP 补偿优先的规则
- 下文旧版消息格式全集与流程图需服从本节规则解释，若与本节冲突，以本节为准
- 鉴权成功后不再额外要求发送 `auth_result` 作为业务前置步骤；连接建立成功即可进入正常事件收发，必要时仅可作为可选通知事件存在
- 鉴权失败、会话失效、权限不足、限流等系统级失败应优先通过 `system.error` / `system.kickout` 表达，不再新增风格不一致的临时事件名
- 客户端与服务端均不得依赖 URL 之外的首包再次传递凭证，避免鉴权语义分裂
- 历史兼容期结束后，旧 `type` 风格消息应整体移除，不再对外暴露
- 公共聊天室、私聊、系统通知三类通道在事件信封层完全一致，仅 `event` 与 `data` 业务内容不同
- 若服务端推送的事件需要前端进一步通过 HTTP 拉详情，事件中至少应包含可定位资源的主键或业务键，不得只给纯文案
- 心跳、回执、广播、错误、强制下线都属于 WebSocket 事件；登录、历史拉取、补偿同步、文件上传仍归属 HTTP 接口职责
- 此规则自本文档生效起为强制规范，不使用"建议"口径
- 所有新增 WebSocket 接口、流程图、时序说明、前端联调说明均必须引用本节作为统一契约来源
- 若实现层暂时保留旧字段名，应在网关层完成兼容转换，对外输出仍必须遵循本节契约
- 任何 WebSocket 事件设计变更都必须同步检查 HTTP 接口、错误码、鉴权、设备会话、幂等与补偿规则是否一致
- `system.kickout` 的 `reason` 必须采用固定枚举值，对外字段名固定，至少包含：`logout`、`password_changed`、`device_removed`、`account_disabled`、`admin_forced_offline`、`device_session_invalid`
- 若客户端在断线后 5 分钟内重连，仍需先检查本地 ws_token 是否有效；即使 ws_token 未过期，若 `device_session_id` 已失效，也必须视为不可重连
- 所有消息发送相关事件若涉及文件，仍只传 `file_id` 或统一 File Resource，不直接传裸路径或长期可访问 URL
- 对外接口文档、SDK、前端常量定义、测试用例中的事件名与字段名均必须以本节为准保持一致
- 本节为第 5 项 WebSocket 事件协议的最终统一口径
- 所有第 5 项相关内容均已按确认方案落地，不再使用"建议"措辞
- 如旧流程图中仍出现 `auth_result`、`missed_messages`、`heartbeat`、`heartbeat_ack` 等名称，应理解为历史名，不影响本节强制口径
- 后续若需精简旧示例，可直接按本节映射进行替换，不得再引入第二套事件命名体系
- 本节优先级高于下文所有 WebSocket 示例、流程图与实现建议
- 本节同时约束用户端、管理端（如后续有 WS）、公共聊天室、私聊、通知推送等所有 WebSocket 场景
- 任何需要"讨论后落实"的第 5 项内容，现已根据你的确认全部写入本节
- 若未来对协议做版本升级，应显式增加版本字段或网关适配，不得在同一协议版本中静默混用新旧字段
- 本节新增完成

【客户端→服务端消息格式全集】

1. 心跳包（客户端→服务端）
{
  "action": "ping",
  "request_id": "req_ping_xxx",
  "ts": 1709692800,
  "data": {}
}

2. 发送消息（客户端→服务端）
{
  "action": "message.send",
  "request_id": "req_msg_123456",
  "ts": 1709692800,
  "data": {
    "to_user_id": 10087,
    "conversation_key": "10086_10087",
    "job_id": 1,
    "content": "消息内容",
    "message_type": "text",  // text文本/emoji表情/image图片/file文件
    "client_msg_id": "msg_123456"  // 客户端消息ID，用于去重
  }
}

3. 已读回执（客户端→服务端）
{
  "action": "message.read",
  "request_id": "req_read_12345",
  "ts": 1709692805,
  "data": {
    "conversation_key": "10086_10087",
    "last_read_msg_id": 12345,
    "read_time": "2024-03-06 10:00:00"
  }
}

4. 加入会话（客户端→服务端）
{
  "action": "join.conversation",
  "request_id": "req_join_12345",
  "ts": 1709692810,
  "data": {
    "conversation_key": "10086_10087"
  }
}

5. 离开会话（客户端→服务端）
{
  "action": "leave.conversation",
  "request_id": "req_leave_12345",
  "ts": 1709692815,
  "data": {
    "conversation_key": "10086_10087"
  }
}

6. 输入状态（客户端→服务端，可选）
{
  "action": "conversation.typing",
  "request_id": "req_typing_12345",
  "ts": 1709692820,
  "data": {
    "conversation_key": "10086_10087",
    "is_typing": true  // true开始输入/false停止输入
  }
}

7. 消息撤回（客户端→服务端）
{
  "action": "message.recall",
  "request_id": "req_recall_12345",
  "ts": 1709692825,
  "data": {
    "message_id": 12345,
    "conversation_key": "10086_10087"
  }
}

8. 消息删除（客户端→服务端）
{
  "action": "message.delete",
  "request_id": "req_delete_12345",
  "ts": 1709692830,
  "data": {
    "message_id": 12345,
    "conversation_key": "10086_10087"
  }
}

说明：
- 连接鉴权统一在 WebSocket 握手阶段通过 URL 参数 `ws_token={ws_token}` 完成
- 不再支持连接建立后的首条 `auth` 消息鉴权
- 断线重连后通过 HTTP 接口拉取增量数据，不再定义 `reconnect` 这类 WebSocket 补偿指令作为独立协议前置步骤
- 所有写操作必须携带 `request_id`，服务端回执必须原样返回该值

【服务端→客户端推送格式全集】

1. 消息发送回执（服务端→客户端）
{
  "event": "message.sent",
  "request_id": "req_msg_123456",
  "ts": 1709692801,
  "data": {
    "client_msg_id": "msg_123456",
    "message_id": 12345,
    "conversation_key": "10086_10087",
    "status": "sent"  // sent已发送/delivered已送达
  }
}

2. 新消息推送（服务端→客户端）
{
  "event": "message.created",
  "request_id": null,
  "ts": 1709692801,
  "data": {
    "message_id": 1,
    "client_msg_id": "msg_123456",  // 用于客户端确认发送成功
    "from_user_id": 10086,
    "from_user_name": "张三",
    "avatar_file": {
      "file_id": "f_20240306_avatar004",
      "disk": "public",
      "path": "avatars/2024/03/06/avatar004.jpg",
      "access": "public",
      "url": "https://...",
      "temporary_url": null,
      "mime_type": "image/jpeg",
      "size": 99840,
      "original_name": "avatar.jpg"
    },
    "to_user_id": 10087,
    "conversation_key": "10086_10087",
    "job_id": 1,
    "job_title": "电工招聘",
    "content": "消息内容",
    "message_type": "text",
    "created_at": "2024-03-06 10:00:00",
    "is_self": false  // 是否自己发送的消息
  }
}

3. 已读回执推送（服务端→客户端）
{
  "event": "message.read",
  "request_id": "req_read_12345",
  "ts": 1709693100,
  "data": {
    "conversation_key": "10086_10087",
    "reader_id": 10087,
    "reader_name": "李四",
    "last_read_msg_id": 12345,
    "read_time": "2024-03-06 10:05:00",
    "unread_count": 0  // 对方未读数
  }
}

4. 系统通知推送（服务端→客户端）
{
  "event": "notification.created",
  "request_id": null,
  "ts": 1709692801,
  "data": {
    "notification_id": 1,
    "type": "system",  // system系统/application报名/message消息/audit审核
    "title": "系统通知",
    "content": "您的职位已通过审核",
    "link_url": "https://...",
    "created_at": "2024-03-06 10:00:00",
    "unread_count": 5  // 总未读数
  }
}

5. 输入状态推送（服务端→客户端，可选）
{
  "event": "conversation.typing",
  "request_id": null,
  "ts": 1709692820,
  "data": {
    "conversation_key": "10086_10087",
    "user_id": 10087,
    "user_name": "李四",
    "is_typing": true
  }
}

6. 消息撤回推送（服务端→客户端）
{
  "event": "message.recalled",
  "request_id": "req_recall_12345",
  "ts": 1709693400,
  "data": {
    "message_id": 12345,
    "conversation_key": "10086_10087",
    "recall_by": 10086,
    "recall_time": "2024-03-06 10:10:00"
  }
}

7. 消息删除推送（服务端→客户端）
{
  "event": "message.deleted",
  "request_id": "req_delete_12345",
  "ts": 1709693410,
  "data": {
    "message_id": 12345,
    "conversation_key": "10086_10087",
    "deleted_by": 10086,
    "delete_time": "2024-03-06 10:10:10"
  }
}

8. 在线状态推送（服务端→客户端）
{
  "event": "presence.updated",
  "request_id": null,
  "ts": 1709692200,
  "data": {
    "user_id": 10087,
    "is_online": true,
    "last_online_time": "2024-03-06 09:50:00"
  }
}

9. 在线人数统计（服务端→客户端）
{
  "event": "presence.count",
  "request_id": null,
  "ts": 1709692801,
  "data": {
    "total": 328,
    "seeker": 256,
    "employer": 72
  }
}

10. 错误事件（服务端→客户端）
{
  "event": "system.error",
  "request_id": "req_msg_123456",
  "ts": 1709692801,
  "code": 9001,
  "msg": "WebSocket Token无效或过期",
  "data": null
}

11. 强制下线事件（服务端→客户端）
{
  "event": "system.kickout",
  "request_id": null,
  "ts": 1709692801,
  "code": 9001,
  "msg": "登录状态已失效",
  "data": {
    "reason": "device_session_invalid"
  }
}

12. 断线后 HTTP 增量同步（客户端行为说明）
- 重连成功后，客户端必须基于最近游标、`last_msg_id`、`last_notification_id` 等信息通过 HTTP 接口拉取增量数据
- WebSocket 通道仅恢复实时事件流，不承担 `missed_messages` 这类离线补偿专用事件职责
- 客户端需按业务主键、服务端时间与幂等键进行合并，避免重复展示或乱序

【心跳机制】
- 客户端每30秒发送：{"action":"ping","request_id":"req_ping_xxx","ts":1709692800,"data":{}}
- 服务端响应：{"event":"pong","request_id":"req_ping_xxx","ts":1709692800,"data":{"server_time":"2024-03-06 10:00:00"}}
- 连续90秒未收到有效心跳响应，视为断线，触发重连

【断线重连与消息补偿】
1. 客户端检测到断线后，立即尝试重连
2. 重连前先检查当前 ws_token 与 `device_session_id` 是否仍有效；任一失效都必须重新走 HTTP 登录态/GET /api/ws/config 流程
3. 重连成功后，客户端基于最近游标、last_msg_id、last_notification_id 等信息通过 HTTP 接口拉取增量数据
4. 客户端按业务主键与时间顺序做幂等合并，避免重复与乱序
5. WebSocket 通道不再承担 `missed_messages` 这类离线全量补偿职责

【错误事件格式】
{
  "event": "system.error",
  "request_id": "req_xxx",
  "ts": 1709692800,
  "code": 9001,  // WebSocket专用错误码
  "msg": "错误描述",
  "data": {
    "details": {}
  }
}

WebSocket专用错误码：
- 9000: WebSocket连接错误
- 9001: WebSocket Token无效或过期（鉴权失败，仅在WebSocket通道使用，HTTP接口仍使用3001）
- 9002: WebSocket消息格式错误
- 9003: WebSocket发送消息过于频繁
- 9004: WebSocket消息内容违规
- 9005: WebSocket对方已拉黑你
- 9006: WebSocket对方不在线（仅用于特定场景）
- 9007: WebSocket会话不存在或已关闭
- 9008: WebSocket消息撤回超时（超过2分钟）
- 9009: WebSocket无权执行此操作

【鉴权失败处理】
- 握手阶段鉴权失败：服务端返回 `system.error`（code=9001）或按网关能力直接拒绝连接
- 已建立连接后若发现 `device_session_id` 失效：服务端必须先发送 `system.kickout`，再主动断开连接
- 前端收到 `system.error` / `system.kickout` 后，必须立即清理本地登录态与连接状态，并重新走 HTTP 登录/刷新配置流程

2.5 文件存储
------------
存储方式：本地服务器存储（Laravel Filesystem 本地磁盘）
上传限制：
- 单张图片 ≤ 5MB
- 最多3张图片
- 支持格式：jpg, jpeg, png, gif
处理流程：
1. 自动压缩
2. 生成缩略图
3. 添加平台水印（右下角，水印内容为平台名称+域名，透明度30%，字体14px白色）

【Laravel 11 文件存储规范】
- 统一通过 Laravel Filesystem 操作文件，默认使用 `storage/app/public` 磁盘，并通过 `php artisan storage:link` 暴露到 `public/storage`
- 业务表默认保存 `file_id` 或受控的相对 path；对外接口统一输出 File Resource 对象，不得直接向前端暴露裸路径或长期有效 URL，也不建议在业务接口中长期保存原始 base64 内容
- 上传入口统一使用 `multipart/form-data`；控制器接收 `UploadedFile` 对象，交由 FormRequest 校验后再进入文件服务类处理
- 图片压缩、缩略图、水印、违规审核等流程建议封装为独立 Service/Job，避免控制器内堆积文件处理逻辑
- 涉及身份证、营业执照等敏感资质文件时，应区分"公开访问文件"与"受控访问文件"：敏感文件默认存储在非公开目录，通过授权下载接口或临时签名 URL 访问
- 【敏感文件访问控制规范】
  - 访问方式：通过授权下载接口获取临时签名URL
  - 签名URL有效期：15分钟（可配置）
  - 生成方式：由服务端按需生成，Laravel实现为 Storage::disk('private')->temporaryUrl($path, $expiry)
  - 权限校验：验证用户身份及文件访问权限后才返回签名URL
  - 禁止在任何接口响应中直接返回私有文件的真实存储路径
- 目录建议按业务域划分：`companies/`、`resumes/`、`jobs/`、`reports/`；文件名使用 UUID/哈希值生成，避免原始文件名泄露
- 若后续迁移到 OSS / COS / S3，仅需切换 Filesystem 磁盘配置，不改业务接口协议

2.6 域名与服务器
----------------
域名：m.mokag.top
服务器：需支持PHP 8.2、MySQL 8.0+、WebSocket、Redis

【PHP 8.2 环境要求】
- PHP版本：>= 8.2.0
- 必需扩展：pdo_mysql, mbstring, openssl, json, fileinfo, ctype, xml, bcmath, tokenizer
- 可选扩展：redis, swoole (用于高性能WebSocket)
- Web服务器：Nginx 1.20+ 或 Apache 2.4+ (需配置URL重写)
- 数据库：MySQL 8.0+ 或 MariaDB 10.6+
- 缓存：Redis 6.0+ (推荐，用于会话、缓存、队列)

【Laravel 11.x 特有配置】
- 应用目录结构遵循 Laravel 11 标准：app/、bootstrap/、config/、database/、public/、resources/、routes/、storage/
- 应用装配入口为 bootstrap/app.php；服务提供者清单维护于 bootstrap/providers.php
- 环境配置统一使用 .env 与 config/*.php，支持开发/测试/生产多环境切换与配置缓存
- API 路由位于 routes/api.php；控制台命令与调度定义位于 routes/console.php；广播、事件、队列、异常等能力按 Laravel 11 约定接入
- 队列驱动支持：database / redis / sqs；缓存驱动支持：file / redis / memcached；会话驱动支持：file / redis / database
- 生产环境建议启用 `php artisan config:cache`、`php artisan route:cache`、`php artisan view:cache`、`php artisan event:cache` 等优化命令

2.7 职位编号生成规则
--------------------
格式：JOB + YYYYMMDD + 6位序号
示例：JOB202403060001
说明：
- 序号按自然日重置，每天从0001开始
- 使用数据库事务 + SELECT FOR UPDATE 保证并发安全
- 备用方案：使用Redis原子自增，key格式 job_no:20240306，有效期24小时

================================================================================
